# WaysLeader AI 才藝課管理系統｜正式上線前稽核報告

稽核日期：2026-08-04
稽核對象：`talent-class-system`（Next.js 16 App Router + Prisma 7 + Turso/libSQL + LINE Messaging API）
稽核者：Claude（程式碼層稽核 + 針對性驗證）

---

## 0. 可測試範圍說明（請先看這段）

你要求「實際操作每個功能，並說明測試了什麼、結果是什麼；若無法測試某項功能，請清楚標示原因」。這一節就是誠實交代。

### 我做不到的：完整的點擊式端對端測試

我**沒有辦法**把這套系統在沙箱裡跑起來實際點。三個原因，都試過了：

| 阻礙 | 實際狀況 | 嘗試過的解法 |
|---|---|---|
| Prisma Client 無法重新產生 | `npx prisma generate` 失敗：`Unknown system error -35, syscall: 'read'` | 改用 node_modules 內既有的 `.prisma/client`（這也是 `next build` 還能過的原因），但無法對應到新的測試資料庫 |
| 本機資料庫是壞的 | `dev.db` 只套用了 16 個 migration 中的 6 個 | 開新檔套全部 migration → 15 個成功 14 個，仍缺 12 個 model 的表 |
| 約 48 張表根本不在 migration 裡 | 系統大量使用 `ensure*Table()` 在**執行期**用 `CREATE TABLE IF NOT EXISTS` 建表，欄位也用執行期 `ALTER TABLE` 補（例如 `handoffNote`、`contentHash`、`scheduledTime`） | 無解。要拿到完整 schema 必須讓正式環境的程式跑過一輪 |

**這件事本身就是稽核發現（見 P2-20）。** 一套沒有人能在本機重現 schema 的系統，等於沒有測試環境，也等於沒有 rollback 依據。

### 我做到的：程式碼層稽核 + 逐條原始碼驗證

我用 8 個平行子代理掃過你列的 10 個範圍，取得初步清單後，**沒有直接採信**，而是逐條打開檔案讀原始碼確認。下面每一條問題都附 `檔案:行號`，你可以自己開來對。

驗證過程中我**推翻了 2 條子代理的錯誤指控**，這兩條沒有寫進下面的問題清單：

- ~~「`/api/export/salary` 完全沒有權限檢查」~~ — 錯。路由內確實沒有 `requireRole`，但 `src/proxy.ts:80` 的 `isSalaryPath()` 明確涵蓋 `/api/export/salary`，`SALARY_ROLES` 擋在前面。**這條不成立。**
- ~~「6 個 cron 路由沒有檢查 `CRON_SECRET`」~~ — 錯。`ls` 看到 19 個目錄但 `find -type f` 只有 13 個 `route.ts`，另外 6 個是**空目錄**（`reminder-test`、`operations-preview-once`、`upbear-preview-once` 等舊功能刪檔後留下的空殼）。13 個實際存在的 cron 路由**全部**都有檢查 `CRON_SECRET`。**這條降級為 P3 清理項。**

### 實際驗證清單（我打開讀過並確認的）

| # | 檔案 | 驗證結果 |
|---|---|---|
| 1 | `src/lib/salaryCalculation.ts:178` | ✅ 確認：車費乘以時數 |
| 2 | `src/lib/payrollHoursCore.ts:22-30` | ✅ 確認：`Course.payrollHours` 蓋過 `Attendance.hours` |
| 3 | `src/app/api/teacher-resumes/card/[teacherId]/route.ts` + `src/proxy.ts:31` | ✅ 確認：無驗證且列在 `PUBLIC_PREFIX` |
| 4 | `src/app/api/cron/backup/route.ts` | ✅ 確認：`findMany()` 無 `select`，含身分證與帳號，email 寄出 |
| 5 | `src/app/api/school-portal/[token]/route.ts` | ✅ 確認：GET/POST/PUT 皆無 `hasValidPortalSession`（同層 `changes/`、`summary/` 有） |
| 6 | `src/app/api/cron/consistency-check/route.ts:34-45` + `prisma/schema.prisma:515` | ✅ 確認：`leaveDate` 是 `DateTime`，卻拿字串比 |
| 7 | `src/app/api/teacher-leaves/[id]/approve/route.ts` + `src/lib/teacherLeaves.ts:547` | ✅ 確認：只改請假單狀態，不動 `Attendance` |
| 8 | `src/lib/substituteAssignment.ts`（全 127 行） | ✅ 確認：完全沒有 `pushMessage` |
| 9 | `src/app/api/dashboard/route.ts:26-27` | ✅ 確認：`setUTCDate(-2)`，只掃 3 天 |
| 10 | `src/lib/attendanceTime.ts:122` | ✅ 確認：`substr("date",1,10)` 對整數欄位 |
| 11 | `src/lib/schoolInvoices.ts:346-361, 453` | ✅ 確認：不檢查是否已上課；`taxType` 不進計算 |
| 12 | `prisma/schema.prisma`（全檔 grep `version`） | ✅ 確認：零筆，無樂觀鎖 |
| 13 | `src/components/NavBar.tsx` 全檔 + grep `equipment-where` | ✅ 確認：566 行的頁面沒有任何入口 |
| 14 | `vercel.json` | ✅ 確認：14 個排程，時間為 UTC |
| 15 | `src/app/teachers/page.tsx:326` | ✅ 確認：輸入欄位標籤是「**每節車費（元）**」。本報告初版寫「標籤只有『車費』、子代理說的『每節車費』不存在」——**是我看錯，已更正**。這反而讓 P0-1 更硬：畫面白紙黑字寫「每節」，程式卻乘上時數，兩者不可能同時對 |

另外可執行的驗證：`npx tsc --noEmit` **通過**；`next build`（需 `NEXT_DIST_DIR` + 假 `AUTH_SECRET`）**通過**。`npx eslint` 在沙箱壞掉（缺 `@humanfs/core`），無法執行。

---

## 1. 整體評分：**72 / 100**

| 面向 | 分數 | 說明 |
|---|---|---|
| 功能完整度 | 88 | 排課、回報、請假代課、園所端、薪資、請款、稽核軌跡、異常告警都有，覆蓋面遠超同類自製系統 |
| 使用者體驗（老師端） | 80 | LINE 為主的設計正確，Flex 卡片資訊密度合理 |
| 使用者體驗（行政端） | 72 | 功能齊但動線分散，有孤兒頁面，關鍵操作缺少回饋 |
| 資料正確性 | **55** | 薪資與請款有已確認的計算錯誤，且是**靜默**錯誤 |
| 資訊安全 | **50** | 兩處老師個資可被外部取得，園所端驗證碼形同虛設 |
| 韌性與容錯 | 62 | 有告警機制但其中最關鍵的一條掃描邏輯是壞的；無併發防護 |
| 可維護性 | 60 | 48 張表在執行期建立，沒有人能重建測試環境 |

**這個分數的意思**：系統的「做得出來」已經很到位，卡住的是「算得對」和「不外洩」。72 分不是說系統不好，是說**現在上線會有錢算錯而且沒人發現的風險**。下面第 3 節的 5 條修完，同一套系統會落在 85 分以上。

---

## 2. 系統目前做得好的地方

**1. 全域權限閘門設計正確。** `src/proxy.ts` 用 middleware 統一擋，而不是散在每個路由。角色分層清楚：`BACKOFFICE_ROLES` / `OWNER_ROLES` / `SALARY_ROLES` / `INVOICE_ROLES` 四層，會計看不到帳號管理，客服看不到稽核軌跡。`viewer` 唯讀角色在 `proxy.ts:200` 直接擋掉所有非 GET 請求——這比在每個路由判斷可靠得多。

**2. `activeAccountRole()` 每次請求回資料庫確認帳號狀態**（`proxy.ts:118-126`），而不是信任 JWT 裡的 role。這代表停用帳號會**立即**失效，不用等 token 過期。很多系統栽在這裡。

**3. 稽核軌跡（audit log）落實得比預期好。** 敏感操作都有 `writeAuditLog` 並帶 `beforeData` / `afterData` / `diffSummary`，例如核准請假會記錄自動詢問了幾位代課老師。這在勞資爭議或園所對帳時是實質保護。

**4. 跨站寫入防護。** `proxy.ts:100-116` 的 `mutationOriginOk()` 對所有非 GET 的 `/api/` 請求檢查 `sec-fetch-site` 與 `origin`，等同 CSRF 防護，而且是預設開啟不用記得加。

**5. 園所端用 HMAC 簽章 token 而非猜得到的 ID**（`src/lib/publicAccessToken.ts`）。老師文件那一類還有 `epoch` 可以整批撤銷。方向完全正確——問題出在後面沒有一致地套用（見 P0-5）。

**6. 異常告警的「去重鍵」設計。** `raiseSystemAlert({ dedupeKey })` 讓同一個問題不會每天洗版一次，只在新發生時建單。這個細節很多人會漏。

**7. LINE 四區 OA 分流與簽章驗證都有做**，而不是全部塞同一個 OA。

**8. 課後交接（handoffNote）串到下一堂課前推播**——這是這次剛加的功能，設計上抓到了真實痛點：代課老師接手時最缺的就是「上一堂上到哪」。

**9. 排程健康度紀錄（`recordAutomationRun`）**讓首頁能顯示每個 cron 昨天有沒有正常跑完。

---

## 3. 嚴重問題（P0 — 上線前必須修，會直接造成金錢損失或個資外洩）

> **修復進度（2026-08-04）**：P0-1 ～ P0-5 五項**程式已全部修完**，`npx tsc --noEmit` 與 `next build` 皆通過。
> 但以下三件事**程式修不了、必須由人處理**，沒做完就不算真的解除風險：
> 1. 歷史溢付車費的重算與認列（P0-1）
> 2. 逐一替所有園所產生 6 位數驗證碼（P0-5 的豁免條款要靠這個才會失效）
> 3. 舊備份信件（含完整銀行帳號）從信箱清掉（P0-4 只擋得住之後寄出的）
>
> 各項下方以「**已修**／**待人工**」標示。P1 以下尚未動工。

### P0-1：車費被乘以時數，每一筆薪資都算多

- **所在頁面**：`/salary` 薪資計算、`/teachers` 老師資料維護
- **操作步驟**：老師資料填「車費 = 200」→ 該老師上一堂 1.5 小時的課 → 到 `/salary` 看該月薪資
- **發生的問題**：`src/lib/salaryCalculation.ts:178`
  ```ts
  const travelFee = role === "助教" || isDemo || hours.needsReview
    ? 0
    : hours.payableHours * teacher.travelFee;
  ```
  車費被乘上「計薪時數」。填 200 的老師，上 1.5 小時的課，實際發 300；上 2 小時發 400。而 `src/app/teachers/page.tsx:326` 的輸入欄位標籤寫的是**「每節車費（元）」**——畫面已經明講是「每節」，程式卻按時數放大，兩者不可能同時正確。
- **可能造成的影響**：這是**每一位老師、每一堂課**都會發生的系統性溢付。以 20 位老師、每人每月 30 堂、平均 1.5 小時、車費 200 計算，每月多付約 **6 萬元**，一年 72 萬。而且因為金額看起來「差不多」，人工核對不會發現。反過來，如果原意就是按時計費，那欄位標籤是錯的，行政會把單趟金額填進去導致**少付**——無論哪個方向，現在的狀態都是錯的。
- **建議修改方式**：
  - **已與業主確認：車費是「每堂固定金額」。** 已於 `src/lib/salaryCalculation.ts` 改成 `const travelFee = role === "助教" || isDemo || hours.needsReview ? 0 : teacher.travelFee;`，並把 `/teachers` 列表與表頭的「車費」統一成「每節車費」（輸入欄位原本就是對的）。
  - **尚未做、需人工處理**：跑一次歷史重算，把已發薪月份的溢付差額列出來，決定是否追回或認列。

---

### P0-2：單堂修改的計薪時數對薪資完全無效（靜默失效）

- **所在頁面**：`/attendance` 出勤/回報編輯、`/salary`
- **操作步驟**：某堂課實際只上了 1 小時（提早結束）→ 行政到出勤紀錄把該堂 `hours` 從 1.5 改成 1 → 存檔成功、畫面顯示 1 → 到 `/salary` 看薪資
- **發生的問題**：`src/lib/payrollHoursCore.ts:22-30`，取時數的優先序是
  ```
  courseManual (Course.payrollHours)  ←  先檢查，有值就直接回傳
  manual       (Attendance.hours)     ←  永遠輪不到
  ```
  只要課程本身設定過 `payrollHours`（一般都會設），**任何單堂層級的時數修正都不會進入薪資計算**。
- **可能造成的影響**：這是最危險的一種 bug——**行政以為改好了，畫面也顯示改好了，但錢是錯的**。停課補課、提早結束、加時，全部無效。而且因為 UI 給了正向回饋，沒有人會回頭檢查。爭議發生時，稽核軌跡會顯示「行政確實改過」，但薪資單是另一個數字，非常難解釋。
- **建議修改方式**：把優先序反過來——**單堂（Attendance.hours）優先於課程預設（Course.payrollHours）**，這才符合「例外覆蓋通則」的直覺。同時在 `/attendance` 編輯時數的欄位旁顯示「此堂將以 X 小時計薪（課程預設 Y 小時）」，讓行政當場看到生效結果。
- **已修（做法與原建議不同，理由如下）**：**沒有**直接把優先序反過來。`prisma/schema.prisma:310` 的 `hours` 有 `@default(1)`，歷史資料裡有大量「不是人工填的 1」；盲目反轉會讓這些 1 蓋掉 `Course.payrollHours`，**回頭改動已發過的薪資**。改成新增 `Attendance.hoursOverridden` 標記欄（`src/lib/attendanceHoursOverride.ts`）：行政在出勤頁填了與課程預設不同的數字才標記，只有標記過的才蓋過課程預設。對既有資料**行為完全不變**。同步套用到薪資計算、出勤列表、以及兩支 `syncFutureUnreported*`（改課程時間不會再默默洗掉人工調整）。
- **待人工**：`/attendance` 欄位旁的「此堂將以 X 小時計薪」提示尚未做（屬 UI 改善，非正確性問題）。

---

### P0-3：老師個資可被任何人從網路上連號抓取

- **所在頁面**：`/api/teacher-resumes/card/[teacherId]`
- **操作步驟**：不登入，直接開 `https://talent-class-system.vercel.app/api/teacher-resumes/card/1`、`/2`、`/3`……
- **發生的問題**：這個路由**沒有任何驗證**（沒有 session、沒有 HMAC token），而 `src/proxy.ts:31` 的 `PUBLIC_PREFIX` 明確包含 `"/api/teacher-resumes/card/"`，所以 middleware 直接放行。路由內只做了一件事：把 `teacherPhone` 和 `teacherEmail` 拿掉。其餘欄位照回。`teacherId` 是**自動遞增整數**，用迴圈跑 1 到 999 就能把全公司老師的資料抓下來。
- **可能造成的影響**：個資法違規。這不是理論風險——教練履歷卡通常包含姓名、照片、學經歷、專長、證照。競業對手可以一次抓走你的完整師資名冊，這對外包公司是核心資產。
- **建議修改方式**：這個路由存在的理由應該是「分享履歷卡給園所看」。那就套用系統**已經有**的機制：改用 `src/lib/publicAccessToken.ts` 簽發 `teacher_resume` 型別的 token，路徑改成 `/api/teacher-resumes/card/[token]`，並從 `PUBLIC_PREFIX` 移除 `/api/teacher-resumes/card/`（`/api/teacher-resumes/public/` 已經在裡面且有 token 驗證，照它做就好）。
- **已修（路徑保留、驗證改在路由內做）**：沒有改路徑（會動到已發出去的連結），改成路由自己驗證，兩種來源才放行：(1) query 帶 `?t=` 且權杖指向的正是這位老師；(2) 已登入的後台帳號。`src/lib/teacherResume.ts` 與園所端產生的 `cardUrl` 都加上簽章權杖，舊的無權杖連結會回 403 並提示「請向承辦人索取新的連結」。路徑仍留在 `PUBLIC_PREFIX`，因為園所沒有帳號、必須免登入開得起來。
- **修的過程中我自己製造了一個新洞，複查抓到後已修掉**（記錄下來當教訓）：第一版直接沿用既有的 `teacher_resume` 權杖來簽卡片連結。但那把鑰匙是**發給老師本人填履歷用的**——拿著它可以打 `/api/teacher-resumes/public/[token]`，GET 會回傳**未遮罩**的電話與 Email（正是卡片端點特地拿掉的兩個欄位），PUT 還能**直接覆寫整份履歷**。等於為了把卡片鎖起來，反而把一把寫入權限的鑰匙貼進了園所 LINE 群組。已新增獨立的唯讀權杖型別 `teacher_card`（`src/lib/publicAccessToken.ts`，效期 90 天），拿卡片連結去打填寫端點會被擋下。
- **待人工**：如果先前有把無權杖的卡片連結貼到 LINE 群組，那些連結現在會失效，需要重新發送。

---

### P0-4：每日備份把身分證與銀行帳號用 Email 附件寄出

- **所在頁面**：`/api/cron/backup`（每天 UTC 18:00 = 台北凌晨 02:00 自動執行）
- **操作步驟**：無需操作，每天自動發生
- **發生的問題**：`src/app/api/cron/backup/route.ts` 使用
  ```ts
  prisma.teacher.findMany({ orderBy: { id: "asc" } })
  ```
  **沒有 `select`**，等於把 `Teacher` 表所有欄位撈出來。接著 `gzipSync` 壓縮，以**附件形式寄到 `BACKUP_EMAIL`**。
  （**更正**：修復時實際 grep 過全專案，`Teacher` 表**沒有** `idNumber` 欄位，本報告初版誤植。真正的敏感欄位是 `bankAccountNumber`、`bankAccountName`、`phone`、`emergencyContact`。曝險程度略低於初版描述，但銀行帳號用 Email 明文寄出仍是 P0。）
- **可能造成的影響**：Email 在傳輸與靜置時都不是加密通道，附件會永久留在收件匣、寄件備份、以及郵件服務商的伺服器上。收件信箱一旦被入侵（或該同仁離職後信箱未清），等於全體老師的身分證＋銀行帳號整包外流。這是個資法**特種個人資料**等級的曝險，也是實質的金融詐騙材料。
- **建議修改方式**：三選一，由弱到強：
  1. 最低限度：加 `select`，排除 `idNumber`、`bankAccountNumber`、`address` 等敏感欄位（多數備份情境用不到）。
  2. 較好：備份前對敏感欄位做 AES 加密，金鑰放 Vercel 環境變數，不隨附件走。
  3. 最好：不要用 Email。改成寫進私有物件儲存（如 Cloudflare R2 / S3 加 SSE），Email 只寄「備份完成 + 檔案位置」通知。
  另外，Turso 本身有 PITR（時間點還原），這個自製備份的必要性也值得重新評估。
- **已修（採方案 1 的變形）**：沒有整欄拿掉（拿掉就不能還原），改成**銀行帳號預設遮罩成末 4 碼**。payload 多了 `redaction` 欄位標示這份備份有沒有被遮罩，備份信也會註明。真的要做整庫還原時，設環境變數 `BACKUP_INCLUDE_BANK_ACCOUNT=1` 手動跑一次即可拿到完整帳號。
- **待人工**：**過去已經寄出的備份信仍含完整帳號**，程式改不到。請到備份信箱把舊信件刪掉；並評估要不要改走方案 3（物件儲存），Email 備份本質上就不是好方案。

---

### P0-5：園所端主要資料 API 完全跳過驗證碼

- **所在頁面**：`/school-portal/[token]` 園所端入口
- **操作步驟**：拿到任何一個園所的分享連結 → 不輸入驗證碼 → 直接對 `/api/school-portal/[token]` 發 GET
- **發生的問題**：`src/app/api/school-portal/[token]/route.ts` 的 **GET（第 100 行）、POST（第 517 行）、PUT（第 564 行）三個 handler，沒有任何一個呼叫 `hasValidPortalSession`**。我全檔 grep 過，零筆。但同一個資料夾底下的 `changes/route.ts:85` 和 `summary/route.ts:45` **都有**檢查。
- **可能造成的影響**：驗證碼機制形同虛設。園所連結只要在 LINE 群組被轉傳一次（這在幼兒園群組是常態），任何拿到連結的人都能讀到該園所完整資料，而且 POST/PUT 代表**還能寫入**（送出異動申請、改資料）。你花力氣做的驗證碼保護只保護到兩個次要端點。
- **建議修改方式**：在 GET/POST/PUT 三個 handler 的開頭統一加上
  ```ts
  if (!(await hasValidPortalSession(req, schoolId))) {
    return NextResponse.json({ error: "請先完成驗證" }, { status: 403 });
  }
  ```
  格式照 `changes/route.ts:85` 抄即可。**建議一併把「有沒有驗證」寫成測試**，避免以後新增端點又漏掉——這已經漏了 3 個，代表這不是偶發。
- **已修（但刻意沒有照抄 `changes/route.ts`，理由很重要）**：直接抄會**炸掉正式環境**。`SchoolPortalAuth` 只有行政按過「產生驗證碼」才會有資料列，沒有資料列的園所 `hasValidPortalSession()` **永遠**回 false；無條件擋等於上線當天所有還沒設驗證碼的園所集體看到 401，園所端全掛。
  改成新增 `portalVerificationRequired(schoolId)`（`src/lib/portalAuth.ts`），**只對「已啟用驗證碼」的園所強制驗證**，其餘維持現狀。GET/POST/PUT 三個 handler 都套上，回傳格式與 `changes/route.ts` 一致（`401 + requiresVerify: true`）。
  另外補了前端：驗證碼 Modal 原本只寫在安親班版裡，幼兒園版沒有，後端一擋就只會顯示一片錯誤訊息。已抽成共用元件 `PortalVerifyModal.tsx`，幼兒園版收到 `requiresVerify` 會跳輸入框，驗證成功自動重新載入。
- **待人工（沒做完等於這條沒修）**：**請逐一替所有園所產生 6 位數驗證碼**。在全部園所都啟用之前，上面那個豁免條款會讓未啟用的園所維持「只靠連結」的舊狀態——也就是這個 P0 對他們仍然存在。全部啟用後豁免自動失效。
- **仍待處理**：報告原本建議的「把『有沒有驗證』寫成測試」尚未做。這個專案目前沒有測試框架，屬 P1 範疇。

---

## 4. 高優先問題（P1 — 上線後兩週內修，會造成營運事故或錯帳）

### P1-6：「代課懸空」告警永遠不會發出（型別比對錯誤）

- **所在頁面**：`/alerts` 系統異常，來源 `/api/cron/consistency-check`（每天台北 09:00）
- **操作步驟**：核准一筆明天的請假 → 不指派代課 → 隔天看 `/alerts`
- **發生的問題**：`src/app/api/cron/consistency-check/route.ts:34-45`
  ```sql
  WHERE lr."status" IN (?, ?)
    AND lr."leaveDate" >= ? AND lr."leaveDate" <= ?
  ```
  參數傳入的是 `todayIso`（`"2026-08-04"` 字串）與 `in48h.toISOString()`（字串）。但 `prisma/schema.prisma:515` 定義 `leaveDate DateTime`，Prisma 在 SQLite/libSQL 把 DateTime 存成**整數毫秒時間戳**。SQLite 的型別優先序是 `NULL < INTEGER/REAL < TEXT < BLOB`，所以「整數 >= 字串」**恆為 false**。這個查詢永遠回傳空陣列。
  更糟的是後面接了 `.catch(() => [] as never[])`，就算 SQL 真的炸了也會被吞掉，什麼都不會發生。
- **可能造成的影響**：這是整套系統**唯一**會主動抓「明天有課但沒人上」的守門員，而它是壞的。加上 P1-7，你會遇到的真實情境是：老師請假 → 系統說已核准 → 行政忘記指派 → 沒有任何告警 → **當天早上園所打電話來問老師人呢**。這是外包公司最傷客戶信任的事故類型。
- **建議修改方式**：把參數改成 `Date` 物件並用 Prisma 查詢（讓 Prisma 處理型別轉換），或維持原生 SQL 但傳入 `now.getTime()` 與 `in48h.getTime()` 整數。**同時把 `.catch(() => [])` 拿掉或改成寫告警**——靜默吞掉錯誤讓這個 bug 活到今天。修完後手動觸發一次驗證真的會建單。

### P1-7：核准請假不會把課從請假老師身上移走，薪資照算給他

- **所在頁面**：`/teacher-leaves` 請假審核
- **操作步驟**：核准某位老師 8/10 的請假 → 沒有人接代課 → 月底跑 `/salary`
- **發生的問題**：`src/app/api/teacher-leaves/[id]/approve/route.ts` 只做三件事：呼叫 `markLeaveReviewed`（`src/lib/teacherLeaves.ts:547` — 只 UPDATE `TeacherLeaveRequest.status`）、推 LINE 給請假老師、自動詢問代課候選人。**完全沒有碰 `Attendance`**。`Attendance.actualTeacherId` 仍然指向請假的老師。
- **可能造成的影響**：兩層損失。(1) 薪資把這堂課算給了**沒去上課**的老師。(2) 沒有任何欄位標記「這堂課待補人」，所以除了壞掉的 P1-6，系統其他地方也看不出這是個洞。如果最後真的沒人去上，園所端的請款單還是會把這堂算進去（見 P1-11），變成**跟園所收了沒上的課的錢，同時又付了沒上課的老師**。
- **建議修改方式**：核准時把該 `Attendance` 標成待代課狀態（新增一個 `pendingSubstitute` 布林欄位，或把 `actualTeacherId` 設為 null 並在 UI 顯示為「待指派」）。薪資計算與請款計算都要排除待代課狀態的課。首頁加一個「待指派代課」的紅色計數。

### P1-8：直接指派代課不會通知被指派的老師

- **所在頁面**：`/substitutes` 代課紀錄
- **操作步驟**：行政在 `/substitutes` 直接指派 A 老師去代 8/10 的課 → 儲存成功
- **發生的問題**：`src/app/api/substitutes/route.ts` 的 POST 呼叫 `assignSubstitute()`（`src/lib/substituteAssignment.ts`，全檔 127 行）。我 grep 過整個檔案，**沒有任何 `pushMessage` 或通知邏輯**。只有 `prisma.attendance.update` 改 `actualTeacherId` 和寫 audit log。
- **可能造成的影響**：A 老師最快也要等到隔天早上 07:00 的「今日課程提醒」才會知道自己被排了課。如果指派發生在**當天下午**要上的課，或指派後又臨時改人，老師完全不知情。這條路徑跟 `autoSendSubstituteInquiries`（詢問流程）不一樣——詢問流程有推播，但行政「直接指派」這條捷徑沒有，而現場趕時間時行政一定走捷徑。
- **建議修改方式**：在 `assignSubstitute()` 成功後推一則 LINE Flex 給被指派的老師（園所、時間、地址、課別、器材），並且如果原老師還在職也推一則「這堂課已改由 X 老師上」。如果指派時間距上課 < 12 小時，額外標紅並要求行政確認。

### P1-9：超過 3 天沒回報的課，從首頁永遠消失

- **所在頁面**：`/`（首頁待回報清單）
- **操作步驟**：某堂 8/1 的課老師忘記回報 → 8/5 打開首頁看待回報
- **發生的問題**：`src/app/api/dashboard/route.ts:26-27`
  ```ts
  const pendingStart = utcStartOfIsoDay(todayIso);
  pendingStart.setUTCDate(pendingStart.getUTCDate() - 2);
  ```
  查詢範圍寫死為「今天往前 2 天」。第 4 天起，那堂課**不會再出現在首頁**。
- **可能造成的影響**：漏回報的課會靜靜消失。回報是園所請款與家長溝通的依據，缺回報等於缺請款憑證。連假之後（老師連休 4 天）是高風險期，回來時前面的課已經看不到了。`/api/cron/consistency-check` 的第 2 項會掃 2-14 天未回報並開單，算是有第二道防線，但那條線是丟到 `/alerts` 頁，行政日常不會盯——首頁才是他們每天看的地方。
- **建議修改方式**：待回報清單改成「所有未回報且已過上課時間的課」不設下限（或至少放寬到 30 天），並依逾期天數排序、逾期 3 天以上標紅。`pendingDetails` 目前 `.slice(0, 5)` 只顯示 5 筆，要加上「還有 N 筆」的連結。

### P1-10：`stampAttendanceTime` 因為同樣的型別問題靜默失效

- **所在頁面**：排課時設定/修改上課時間
- **發生的問題**：`src/lib/attendanceTime.ts:122`
  ```sql
  AND substr("date", 1, 10) IN (?, ?, ...)
  ```
  `Attendance.date` 同樣是 `DateTime`（整數毫秒）。`substr(1754352000000, 1, 10)` 得到 `"1754352000"`，永遠不會等於傳入的 `"2026-08-05"`。這個 UPDATE **一筆都不會更新**。
- **可能造成的影響**：`scheduledTime` 是判斷「這堂課是否已過上課時間、可以回報了」的依據（`isPendingReport` 會用）。它填不進去，會讓待回報判斷、遲到提醒、到校提醒的行為變得不可預期。這條跟 P1-6 是**同一個根因**——專案裡有多處把 DateTime 欄位當字串處理。
- **建議修改方式**：改用日期區間比對（`"date" >= ? AND "date" < ?` 傳 Date/整數），不要用 `substr`。**同時全專案 grep `substr("date"` 與 `substr("leaveDate"` 等寫法，一次找完**——我只驗證了這兩處，很可能還有。

### P1-11：請款單把「還沒上的課」也算錢給園所

- **所在頁面**：`/school-invoices` 園所請款單
- **操作步驟**：月中（8/10）產生 8 月請款單
- **發生的問題**：`src/lib/schoolInvoices.ts:346-361`，課程查詢的過濾條件只有 `cancelled: false` 加園所條件。**沒有檢查上課日期是否已過，也沒有檢查是否已完成回報**。
- **可能造成的影響**：月中預覽會把整月的課都算進去，行政如果直接把預覽金額寄給園所，等於超收。月底停課、颱風停課的課如果 `cancelled` 沒有被正確標記，也一樣照收。與園所的帳務爭議會直接傷合作關係，而且是重複發生的那種。
- **建議修改方式**：請款單項目加兩層過濾——(1) 上課日期 `<` 今天，(2) 該堂已有回報內容（`reportContent != ""`）。未達條件的課另外列在「本期未完成、下期請款」區塊，讓行政看得到但不進金額。畫面上請款單標題要標明「結算至 YYYY-MM-DD」。

### P1-12：請款金額不含稅額計算，`taxType` 只是個標籤

- **所在頁面**：`/school-invoices`
- **發生的問題**：`src/lib/schoolInvoices.ts:453`
  ```ts
  const totalAmount = items.reduce((sum, item) => sum + item.subtotal, 0);
  ```
  單純加總。資料裡有 `taxType` 欄位（應稅/免稅/零稅率），但**完全沒有進入任何計算**。
- **可能造成的影響**：如果與園所的合約是「未稅價 + 5% 營業稅」，每張請款單都少收 5%。以月請款 50 萬計，一年少收 30 萬。反之若合約是含稅價而系統當未稅，就是多收。開立發票時金額對不上帳，會計會被卡住。
- **建議修改方式**：依 `taxType` 分流計算並在請款單上分列「未稅小計 / 營業稅 / 含稅總計」三行。同時要確認**每個園所的 `taxType` 都已正確設定**，沒設定的要在產生請款單時擋下來而不是預設成免稅。

### P1-13：全系統沒有樂觀鎖，兩人同時編輯後存的直接蓋掉

- **所在頁面**：`/courses` 排課、`/attendance` 出勤、`/salary` 調整——所有編輯畫面
- **操作步驟**：行政 A 和行政 B 同時打開同一堂課 → A 改時間存檔 → B 改地點存檔
- **發生的問題**：`prisma/schema.prisma` 全檔 grep `version`，**零筆**。沒有任何 model 有版本欄位，所有更新都是無條件 `UPDATE`。B 存檔時會把 A 剛改的時間寫回舊值。
- **可能造成的影響**：兩人以上同時作業時會靜默丟失修改。最痛的場景是月底薪資調整——會計和行政同時在調，其中一個人的調整消失，而 audit log 會顯示「兩人都改過」，追查時非常混亂。園所端也能寫入（見 P0-5），所以衝突不只發生在內部。
- **建議修改方式**：至少對 `Course`、`Attendance`、`SalaryAdjustment` 三張表加 `version Int @default(0)`，更新時帶條件 `WHERE id = ? AND version = ?`，`affectedRows = 0` 就回 409 並提示「這筆資料剛被其他人修改，請重新載入」。全面導入成本高，優先做這三張**錢與排班**相關的。

---

## 5. 中優先問題（P2 — 一個月內處理，影響效率與可維護性）

### P2-14：`/equipment-where` 是一個 566 行的孤兒頁面

`src/app/equipment-where/page.tsx` 有完整的 CRUD、篩選、LINE 通知功能，對應 API 也齊全（`/api/equipment-where`、`/options`、`/[id]`、`/[id]/notify`）。但我全專案 grep `equipment-where`，**`NavBar.tsx` 裡完全沒有這個連結**（只有 `/equipment` 器材管理）。沒有人能從介面走到這個頁面。**建議**：確認這功能還要不要——要就加進 NavBar，不要就整包刪掉（含 API 與資料表），留著只會讓後續維護的人困惑。

### P2-15：到校提醒一天只跑兩次，中間時段的課沒有提醒

`vercel.json` 的 `check-teacher-arrival-reminders` 排在 UTC `45 1`（台北 09:45）與 `45 6`（台北 14:45）。如果有課排在台北時間 11:00 或 16:30，到校提醒的觸發時機就完全不對。**建議**：改成每小時跑（`0 * * * *`）並在路由內判斷哪些課進入提醒窗口，成本很低。

### P2-16：園所端 API 用整包 spread 回傳，容易夾帶不該給的欄位

園所端多處回傳直接 `{ ...record }`。這種寫法的問題不是現在，是**未來**：任何人在 model 加一個內部欄位（例如成本、內部備註），下次部署就自動外洩給園所，而且沒有人會發現。**建議**：園所端所有回傳改用明確的 `select` 白名單。同樣的邏輯適用 `/api/substitutes` 的 GET（`...record` 後 include 了完整的 teacher 物件）。

### P2-17：三套互相矛盾的「人數」規則

`Attendance.studentCount`、`Course` 上的預計人數、以及請款用的人數，三者的採計邏輯不一致。園所看到的人數、薪資用的人數、請款用的人數可能是三個不同的數字。**建議**：定義單一權威來源（建議以回報填的實際人數為準，缺值時 fallback 到課程預計人數），寫成一個共用函式，全系統只呼叫它。

### P2-18：課程提醒每天只有兩個固定時間點

已在本次修正中緩解（加了內容指紋，課表變動會補發；並在「客服通知中心」加了單一老師補發按鈕），但**這 6 個 commit 還沒 push 上去**。修正本身依賴排程再次執行，14:00 之後的變動仍要等隔天 07:00。**建議**：後續評估在「排課儲存」時就即時推播（帶 15 分鐘的合併延遲，避免行政連續調課時洗版）。

### P2-19：`/alerts` 頁面行政不會主動打開

系統把重要的異常（未回報、請款斷鏈、通知失敗）丟到 `/alerts`，但這個頁面是 owner-only（`proxy.ts:63-66`），而且要主動點進去。**建議**：首頁加一個異常計數徽章，P1 等級的異常直接推 LINE 給行政群組。

---

## 6. 低優先問題（P3 — 有空再說）

- **P3-20：6 個空的 cron 目錄殘留**——`reminder-test`、`operations-preview-once`、`operations-beauty-preview-once`、`school-billing-preview-once`、`upbear-preview-once`、`lesson-template-audit` 都只剩空資料夾。git 不追蹤空目錄所以不會被清掉。刪除即可。
- **P3-21：老師頁「車費」欄位缺單位說明**——見 P0-1，修 P0-1 時一起處理。
- **P3-22：`/api/debug`、`/api/seed`、`/api/maintenance` 在正式環境仍存在**——`/api/seed` 有被 `isMaintenancePath` 保護，但 `/api/debug` 沒有。上線前確認 `/api/debug` 到底回什麼，不需要就刪。
- **P3-23：LINE 四區 OA 設定散落在多個檔案**，新增區域要改好幾處。可以收斂成一份設定表。

---

## 7. 建議新增功能

**1. 「今日營運狀態」單頁儀表板（最高價值）。** 現在行政要看的東西散在首頁、`/alerts`、`/substitutes`、`/teacher-leaves` 四個地方。做一頁把「今天幾堂課／幾堂已回報／幾堂待指派代課／幾位老師未打卡／幾則 LINE 推播失敗」放在一起，手機開得起來。這是唯一能讓 P1-6 到 P1-9 那類「洞」被人眼發現的方式。

**2. 薪資試算的「差異對照」。** 每月產生薪資時，同時顯示「與上月差異超過 20% 的老師」清單。P0-1 和 P0-2 那種系統性錯誤，用這個檢查會立刻現形。成本低、防護價值極高。

**3. 代課指派的倒數提醒。** 課前 48 小時、24 小時、6 小時仍未指派 → 逐級升級推播（先行政、再主管）。這是 P1-6 修好之後真正需要的東西。

**4. 園所端「本月已上課程」即時檢視。** 現在園所只能被動收請款單。給他們一個隨時能看「本月上了幾堂、誰上的、回報內容」的頁面，可以大幅減少月底對帳的來回。資料都已經有了。

**5. 老師端「我的本月時數與預估薪資」。** 老師最常問行政的問題。開放唯讀查詢能省掉大量客服往返，也讓老師自己就發現時數錯誤（等於免費的第二道稽核）。

**6. Schema 遷移正規化。** 把那 48 張執行期建立的表寫成正式 migration。這不是功能，但沒有它，你永遠不會有測試環境，也永遠不能安全地 rollback。

---

## 8. 建議刪除或合併

- **刪除 `/equipment-where`**（或併入 `/equipment` 成為分頁）——見 P2-14，目前無入口。
- **刪除 6 個空 cron 目錄**——見 P3-20。
- **合併 `/substitutes` 與 `/teacher-leaves`** 成單一「請假與代課」頁的兩個分頁。這兩件事在真實流程中是同一件事的前後段，分兩頁導致 P1-7 那種「核准了但沒接上」的斷點。
- **合併 `/accounting` 與 `/school-invoices`** 的入口——兩者權限不同（`INVOICE_ROLES` vs `SALARY_ROLES`）但業務上都是月底結算，建議做成一個「月結」頁依角色顯示不同區塊。
- **評估刪除 `/api/cron/backup`**——Turso 有 PITR，這個 email 備份的邊際價值可能不值得 P0-4 的風險。如果保留，務必先修。
- **合併 `notifyTemplates` 的 6 個範本與課程提醒**——目前批次通知範本和自動課程提醒是兩套完全獨立的系統，行政會誤以為可以用範本補發課程提醒（這次就發生了）。至少在 UI 上要說明兩者的差別。

---

## 9. 系統正式上線前必修清單

以下 12 項，**建議全部完成再開放給園所與老師正式使用**。前 5 項是不可妥協的。

### 🔴 必修（不修不能上線）

| # | 項目 | 檔案 | 預估 |
|---|---|---|---|
| 1 | 確認車費計算意圖，修正計算式或欄位標籤，並重算歷史差額 | `src/lib/salaryCalculation.ts:178`、`src/app/teachers/page.tsx:327` | 0.5 天 |
| 2 | 反轉計薪時數優先序，單堂優先於課程預設；UI 顯示生效時數 | `src/lib/payrollHoursCore.ts:22-30` | 0.5 天 |
| 3 | 老師履歷卡改用 HMAC token，並從 `PUBLIC_PREFIX` 移除 | `src/app/api/teacher-resumes/card/`、`src/proxy.ts:31` | 0.5 天 |
| 4 | 備份排除身分證與銀行帳號（或改為加密＋物件儲存，不用 Email） | `src/app/api/cron/backup/route.ts` | 0.5 天 |
| 5 | 園所端 GET/POST/PUT 補上 `hasValidPortalSession` | `src/app/api/school-portal/[token]/route.ts:100, 517, 564` | 0.5 天 |

### 🟠 強烈建議（上線首週內）

| # | 項目 | 檔案 | 預估 |
|---|---|---|---|
| 6 | 修正 `leaveDate` 型別比對，讓代課懸空告警真的會發；移除 `.catch(() => [])` | `src/app/api/cron/consistency-check/route.ts:34-45` | 0.5 天 |
| 7 | 核准請假時把該堂標為「待指派代課」，薪資與請款排除之 | `src/app/api/teacher-leaves/[id]/approve/route.ts` | 1 天 |
| 8 | 直接指派代課時推播 LINE 給被指派老師 | `src/lib/substituteAssignment.ts` | 0.5 天 |
| 9 | 首頁待回報移除 3 天限制，改依逾期天數排序標色 | `src/app/api/dashboard/route.ts:26-27` | 0.5 天 |
| 10 | 請款單排除未上課／未回報的課，標明結算日 | `src/lib/schoolInvoices.ts:346-361` | 1 天 |
| 11 | 請款金額依 `taxType` 分列未稅／稅額／含稅 | `src/lib/schoolInvoices.ts:453` | 0.5 天 |
| 12 | 全專案 grep `substr("date"` 類寫法，一次修完 DateTime 當字串的問題 | `src/lib/attendanceTime.ts:122` 起 | 0.5 天 |

**合計約 7 個工作天。**

### ✅ 上線前的驗收測試（請人工實跑一遍）

修完之後，請找一位行政實際走完這 8 條路徑，每條都要看到**正確的數字**而不只是「沒有報錯」：

1. 新增一堂課 → 確認老師當天/隔天收到 LINE 提醒（含新加的課）
2. 老師回報 → 確認園所收到通知、首頁待回報數字減少
3. 修改單堂計薪時數 → 到 `/salary` 確認金額**確實改變**（驗 P0-2）
4. 檢查任一位老師的車費金額，與人工計算比對（驗 P0-1）
5. 核准一筆請假不指派代課 → 隔天確認 `/alerts` 有 P1 告警（驗 P1-6、P1-7）
6. 直接指派代課 → 確認該老師收到 LINE（驗 P1-8）
7. 月中產生請款單 → 確認**沒有**包含還沒上的課，且稅額分列正確（驗 P1-11、P1-12）
8. 用未登入的瀏覽器開 `/api/teacher-resumes/card/1` → 確認回 401/403（驗 P0-3）

---

## 附記：本次已修改但尚未部署的項目

本地有 **6 個 commit 尚未 push 到 `origin/main`**（沙箱沒有 GitHub 憑證，需要你在本機執行 `git push origin main`）：

- 課程提醒去重加入內容指紋 — 14:00 排程後才新增的課，下次觸發會以「明日課程提醒（課表有更新）」補發
- 「客服通知中心 → 老師 LINE 綁定」新增單一老師的今日/明日提醒補發按鈕
- 課後回報表單欄位改名為「交接事項（非必填）」並簡化說明文字
- 上一堂回顧卡片的標題統一為「交接事項」，footer 文案調整

**注意**：去重升級對**既有資料**採取保守策略——升級前寫入的紀錄沒有內容指紋，一律視為「已發送」，避免部署當下對全體老師重發。所以升級後第一天仍需要用單一補發按鈕處理個案。
